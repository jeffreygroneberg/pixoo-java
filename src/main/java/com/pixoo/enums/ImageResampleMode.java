package com.pixoo.enums;

/**
 * Enumeration for image resampling modes.
 */
public enum ImageResampleMode {
    PIXEL_ART,  // Nearest neighbor (for pixel art)
    SMOOTH      // Bicubic interpolation (for smooth scaling)
}
